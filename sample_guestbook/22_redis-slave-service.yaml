## https://github.com/kubernetes/examples/blob/master/guestbook/redis-slave-service.yaml

apiVersion: apps/v1
kind: Service
metadata:
  name: redis-slave
  labels:
    app: redis
    role: slave
    tier: backend
spec:
  ports:
  - port: 6379
  selector:
    app: redis
    role: slave
    tier: backend
