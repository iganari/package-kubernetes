apiVersion: v1
kind: Service
metadata:
  name: apple-service
spec:
  type: NodePort
  selector:
    app: apple
  ports:
    - port: 5678 # default port -> refs: https://hub.docker.com/r/hashicorp/http-echo/
